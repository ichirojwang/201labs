#include <stdint.h>
#include <stdio.h>
#include <unistd.h>

struct header {
  uint64_t size;
  struct header *next;
};

int main() {

  void *start = sbrk(128);
  printf("%p\n", start);

  start = sbrk(128);
  printf("%p\n", start);
}
