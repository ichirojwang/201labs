#include <stdint.h>
#include <stdio.h>
#include <unistd.h>

struct header {
  uint64_t size;
  struct header *next;
};



int main() {

  void *start = sbrk(0x5);
  printf("%p", start);

  start = sbrk(0x5);
  printf("%p", start);
}

